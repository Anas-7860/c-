#include <iostream>
#include <string>
using namespace std;
//making aadhar card password generator
void A_password(string name,int year){
    string new_name;
    int new_year=0,i=0;
    while(i<name.length()){
    if(name[i]!=' '){
        new_name += name[i];
    }
    }
    cout<<new_name;
    
}

int main() {

    string name;
    int year;
    cout<<"enter name :";
    getline(cin,name);
    cout<<"enter birth year :";
    cin>>year;
    
    A_password(name,year);
    
 

    return 0;
}
